{
  "name": "ai-browser",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "start": "node apps/api/src/server.mjs",
    "test": "node --test tests/**/*.test.mjs",
    "lint": "node --check apps/api/src/*.mjs packages/*/src/**/*.mjs"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
